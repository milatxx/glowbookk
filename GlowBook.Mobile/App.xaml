<?xml version="1.0" encoding="utf-8" ?>
<Application
    x:Class="GlowBook.Mobile.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Application.Resources>
        <ResourceDictionary>

            <Color x:Key="GbBabyPink">#ffc1cc</Color>
            <Color x:Key="GbPinkDark">#ffb0bd</Color>
            <Color x:Key="GbWhite">#ffffff</Color>
            <Color x:Key="GbWhite70">#B3FFFFFF</Color>
            <Color x:Key="GbButton">#ec628b</Color>

            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource GbBabyPink}" />
            </Style>

            <Style x:Key="GbPrimaryButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource GbButton}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style x:Key="GbCard" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource GbWhite}" />
                <Setter Property="Padding" Value="16" />
                <Setter Property="Margin" Value="16" />
                <Setter Property="StrokeThickness" Value="0" />
                <Setter Property="StrokeShape">
                    <Setter.Value>
                        <RoundRectangle CornerRadius="24" />
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="GbItemCard" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource GbWhite}" />
                <Setter Property="Padding" Value="12" />
                <Setter Property="Margin" Value="0,6" />
                <Setter Property="StrokeThickness" Value="0" />
                <Setter Property="StrokeShape">
                    <Setter.Value>
                        <RoundRectangle CornerRadius="16" />
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="GbCardTitle" TargetType="Label">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource GbButton}" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
