<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GlowBook.Mobile.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{StaticResource GbBabyPink}">

    <ContentPage.Content>
        <ScrollView>
            <Grid Padding="24">

                <Border Style="{StaticResource GbCard}">
                    <VerticalStackLayout Spacing="14">

                        <Label Text="{Binding Title}"
                               Style="{StaticResource GbCardTitle}" />

                        <Label Text="Instellingen en synchronisatie met de server."
                               FontSize="14"
                               TextColor="Gray" />

                        <!-- API-URL -->
                        <Label Text="API-URL"
                               FontAttributes="Bold" />
                        <Entry Text="{Binding ApiBaseUrl}"
                               Placeholder="https://.../api/"
                               Keyboard="Url" />

                        <Button Text="Opslaan"
                                Style="{StaticResource GbPrimaryButton}"
                                Command="{Binding SaveCommand}" />

                        <BoxView HeightRequest="1"
                                 Color="{StaticResource GbPinkDark}"
                                 Opacity="0.3"
                                 Margin="0,6" />

                        <!-- Sync-status -->
                        <Label FontSize="14">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Verbinding: " FontAttributes="Bold" />
                                    <Span Text="{Binding OnlineText}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>

                        <Label FontSize="14">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Laatste synchronisatie: " FontAttributes="Bold" />
                                    <Span Text="{Binding LastSyncText}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>

                        <Button Text="Nu synchroniseren"
                                Style="{StaticResource GbPrimaryButton}"
                                Command="{Binding SyncNowCommand}" />

                        <ActivityIndicator IsRunning="{Binding IsBusy}"
                                           IsVisible="{Binding IsBusy}"
                                           HorizontalOptions="Center" />

                        <Label Text="{Binding StatusMessage}"
                               TextColor="{StaticResource GbPinkDark}"
                               FontSize="13" />

                    </VerticalStackLayout>
                </Border>

            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
