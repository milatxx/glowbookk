<?xml version="1.0" encoding="utf-8" ?> 
<ContentPage
    x:Class="GlowBook.Mobile.Views.CreateAppointmentPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}"
    BackgroundColor="{StaticResource GbBabyPink}">

    <Grid Padding="24">
        <Border Style="{StaticResource GbCard}">
            <ScrollView>
                <VerticalStackLayout Spacing="12">

                    <Label Text="Nieuwe afspraak" Style="{StaticResource GbCardTitle}" />

                    <Label Text="{Binding Error}" TextColor="Red" />

                    <Label Text="Klant" FontAttributes="Bold"/>
                    <Picker ItemsSource="{Binding Customers}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedCustomer}" />

                    <Label Text="Medewerker" FontAttributes="Bold"/>
                    <Picker ItemsSource="{Binding StaffMembers}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedStaff}" />

                    <Label Text="Service" FontAttributes="Bold"/>
                    <Picker ItemsSource="{Binding Services}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedService}" />

                    <Label Text="Start" FontAttributes="Bold"/>
                    <HorizontalStackLayout Spacing="12">
                        <DatePicker Date="{Binding StartDate}" />
                        <TimePicker Time="{Binding StartTime}" />
                    </HorizontalStackLayout>

                    <Label Text="Einde" FontAttributes="Bold"/>
                    <HorizontalStackLayout Spacing="12">
                        <DatePicker Date="{Binding EndDate}" />
                        <TimePicker Time="{Binding EndTime}" />
                    </HorizontalStackLayout>

                    <Label Text="Status" FontAttributes="Bold"/>
                    <Entry Text="{Binding Status}" />

                    <Button Text="Opslaan (offline/online)"
                            Style="{StaticResource GbPrimaryButton}"
                            Command="{Binding SaveCommand}" />

                    <ActivityIndicator IsRunning="{Binding IsBusy}"
                                       IsVisible="{Binding IsBusy}" />

                </VerticalStackLayout>
            </ScrollView>
        </Border>
    </Grid>
</ContentPage>
