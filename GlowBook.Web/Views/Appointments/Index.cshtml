@model GlowBook.Model.Helpers.PagedResult<GlowBook.Model.Entities.Appointment>

@using Microsoft.Extensions.Localization
@inject IStringLocalizer<GlowBook.Web.SharedResources> SR

@{
    ViewData["Title"] = SR["Calendar_Title"];
    // Filter ophalen uit de ViewBag 
    var filter = (GlowBook.Web.Models.AppointmentFilterViewModel)ViewBag.Filter!;
}

<div class="container mt-4">
    <div class="gb-card">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <div>
                <h2 class="gb-card-title">@SR["Calendar_Title"]</h2>
                <p class="mb-0 text-muted">@SR["Calendar_Subtitle"]</p>
            </div>

            <a asp-action="Create" class="btn-gb">
                @SR["Calendar_NewAppointment"]
            </a>
        </div>

        <!-- FILTER FORM -->
        <form method="get" class="row g-3 mb-4" id="filterForm">
            <div class="col-lg-2 col-md-4">
                <label asp-for="@filter.From" class="form-label"></label>
                <input asp-for="@filter.From" name="From" class="form-control"
                       value="@filter.From?.ToString("yyyy-MM-dd")" type="date" />
            </div>

            <div class="col-lg-2 col-md-4">
                <label asp-for="@filter.To" class="form-label"></label>
                <input asp-for="@filter.To" name="To" class="form-control"
                       value="@filter.To?.ToString("yyyy-MM-dd")" type="date" />
            </div>

            <div class="col-lg-2 col-md-4">
                <label asp-for="@filter.CustomerId" class="form-label"></label>
                <select name="CustomerId" class="form-select" asp-items="filter.Customers">
                    <option value="">@SR["Calendar_AllCustomers"]</option>
                </select>
            </div>

            <div class="col-lg-2 col-md-4">
                <label asp-for="@filter.StaffId" class="form-label"></label>
                <select name="StaffId" class="form-select" asp-items="filter.Staff">
                    <option value="">@SR["Calendar_AllStaff"]</option>
                </select>
            </div>

            <div class="col-lg-2 col-md-4">
                <label asp-for="@filter.Status" class="form-label"></label>
                <select name="Status" class="form-select" asp-items="filter.Statuses">
                    <option value="">@SR["Calendar_AllStatuses"]</option>
                </select>
            </div>

            <div class="col-lg-2 col-md-4 d-flex align-items-end justify-content-end">
                <button type="submit" class="btn-gb me-2">@SR["Calendar_Filter"]</button>
                <a asp-action="Index" class="btn btn-outline-secondary">@SR["Calendar_Reset"]</a>
            </div>
        </form>

        <!-- AJAX-PARTIAL LOADER -->
        <div id="appointmentsContainer">
            @await Html.PartialAsync("_AppointmentsList", Model.Items)
        </div>

        <!-- PAGINERING -->
        <nav aria-label="Page navigation" class="mt-3">
            <ul class="pagination justify-content-center">
                @if (Model.PageNumber > 1)
                {
                    <li class="page-item">
                        <a class="page-link" href="?page=@(Model.PageNumber - 1)">← Vorige</a>
                    </li>
                }

                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                        <a class="page-link" href="?page=@i">@i</a>
                    </li>
                }

                @if (Model.PageNumber < Model.TotalPages)
                {
                    <li class="page-item">
                        <a class="page-link" href="?page=@(Model.PageNumber + 1)">Volgende →</a>
                    </li>
                }
            </ul>
        </nav>
    </div>
</div>

<!-- AJAX SCRIPT -->
@section Scripts {
    <script>
        // Elke keer dat een filter verandert -> AJAX reloadttabel
        $("#filterForm").on("change", "input, select", function () {

            $.ajax({
                url: '@Url.Action("ListPartial", "Appointments")',
                type: 'GET',
                data: $("#filterForm").serialize(),
                success: function (result) {
                    $("#appointmentsContainer").html(result);
                }
            });

        });
    </script>
}
