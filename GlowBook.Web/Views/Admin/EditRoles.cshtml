@model GlowBook.Web.Models.Admin.EditUserRolesViewModel
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<GlowBook.Web.SharedResources> SR

@{
    ViewData["Title"] = SR["Users_EditRolesTitle"].Value;
}

<div class="gb-card mt-3">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <div class="gb-card-title">@SR["Users_EditRolesTitle"]</div>
    </div>

    <p class="text-muted mb-3">
        <strong>@Model.DisplayName</strong> (@Model.Email)
    </p>

    <form method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="UserId" />

        <div class="mb-3">
            @for (int i = 0; i < Model.Roles.Count; i++)
            {
                <div class="form-check mb-2">
                    <input class="form-check-input" asp-for="Roles[i].IsSelected" />
                    <input type="hidden" asp-for="Roles[i].RoleName" />
                    <label class="form-check-label">@Model.Roles[i].RoleName</label>
                </div>
            }
        </div>

        <button class="btn-gb" type="submit">@SR["Common_Save"]</button>
        <a class="btn btn-outline-secondary ms-2" asp-action="Users">@SR["Common_Cancel"]</a>
    </form>
</div>
